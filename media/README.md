# Media

Planned media layout:

- `blank/` for shared blank U.S. base maps
- `locator/` for region and division locator maps

The repo is currently seeded before media acquisition, so these folders are placeholders.
